#include "prime_lib.h"
#include <iostream>

int main(int argc, char const *argv[]) {
  /*ULLI p;
  std::cin >> p;
  std::cout << miller_rabin(p) << '\n';
  std::cout << prime_trial(p) << '\n';*/

  return 0;
}
